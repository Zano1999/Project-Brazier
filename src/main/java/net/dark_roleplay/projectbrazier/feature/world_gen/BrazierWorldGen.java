package net.dark_roleplay.projectbrazier.feature.world_gen;

import net.dark_roleplay.projectbrazier.ProjectBrazier;
import net.minecraft.core.Registry;
import net.minecraft.core.RegistryAccess;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.level.biome.Biome;
import net.minecraft.world.level.levelgen.GenerationStep;
import net.minecraft.world.level.levelgen.placement.PlacedFeature;
import net.minecraftforge.event.world.BiomeLoadingEvent;

public class BrazierWorldGen {

	public static void biomeLoad(final BiomeLoadingEvent event) {
//		if(access == null) return;
//
//		Biome.BiomeCategory category = event.getCategory();
//		PlacedFeature feature = access.registry(Registry.PLACED_FEATURE_REGISTRY).get().get(new ResourceLocation(ProjectBrazier.MODID, "glimmertail_patch"));
//		if(feature == null) return;
//
//		if(category != Biome.BiomeCategory.NETHER && category != Biome.BiomeCategory.THEEND && category != Biome.BiomeCategory.NONE) {
//			event.getGeneration().addFeature(GenerationStep.Decoration.VEGETAL_DECORATION.ordinal(), () -> feature
//			);
//		}
	}
}
